# Control Flow Graph

MiniCompiler now builds a CFG from IR using basic blocks and control transfers.

The CFG supports:

- basic block construction
- successor and predecessor links
- dominator computation
- loop detection via back edges

