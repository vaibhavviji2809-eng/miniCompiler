[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mini-compiler"
version = "0.1.0"
description = "A tiny compiler pipeline in Python"
requires-python = ">=3.11"

[project.scripts]
mc = "MiniCompiler.cli:main"

