# Bytecode

The bytecode compiler resolves labels into concrete instruction addresses and converts IR opcodes into executable bytecode opcodes.

This stage preserves the same stack-machine model used by the IR.

