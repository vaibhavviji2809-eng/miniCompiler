# Semantic Analysis

Semantic analysis validates:

- undefined variables
- duplicate declarations
- return placement
- function calls
- basic type compatibility
- scope rules

The analyzer uses a symbol table with nested scopes.

