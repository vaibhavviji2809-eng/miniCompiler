# Parser

The parser is a recursive-descent parser with precedence climbing for expressions.

It builds statements and expressions such as:

- function declarations
- blocks
- if statements
- loops
- binary expressions
- call expressions

