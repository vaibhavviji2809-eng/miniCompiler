# Language Design

MiniCompiler uses a small, C-like syntax with:

- `let` for variables
- `fn` for function declarations
- `if` / `else`
- `while`
- `for`
- `return`
- `print`

Expressions support arithmetic, comparisons, boolean logic, calls, and grouping.

